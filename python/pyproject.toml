[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fintech-volume-bars"
version = "0.1.0"
description = "Volume Bars for market-data engineering — aggregate a trade tape into fixed-volume OHLCV bars (an activity clock). Canonical cross-language reference implementation."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "The Fintech Builder", email = "islam.baraka.90@gmail.com" }]
keywords = [
  "fintech", "volume-bars", "bar-construction", "ohlcv", "market-data",
  "market-data-engineering", "tick-data", "activity-clock", "aggregation",
  "trading", "finance", "quant", "algorithm",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Financial and Insurance Industry",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Office/Business :: Financial :: Investment",
  "Topic :: Scientific/Engineering :: Information Analysis",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[project.urls]
"Article" = "https://thefintechbuilder.com/market-data-engineering/bar-construction/volume-bars/"
"Homepage" = "https://thefintechbuilder.com"
"Source" = "https://github.com/IslamBaraka90/Fintech-Volume-Bars-Bar-Construction-algorithm"

[tool.hatch.build.targets.wheel]
packages = ["src/fintech_volume_bars"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
